import VideoGrid from '@/components/VideoGrid';

export default function Work() {
    return (
        <main>
            <VideoGrid titleLabel="// SELECTED WORK" titleMain="Featured" titleHighlight="Reels" />
        </main>
    );
}
